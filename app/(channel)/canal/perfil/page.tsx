import type{Metadata}from"next";import{AdminPage}from"@/components/admin/admin-ui";import{requireChannelUser}from"@/lib/auth/session";import{createAdminClient}from"@/lib/supabase/admin";
export const metadata:Metadata={title:"Mi perfil de canal | Arkad",robots:{index:false,follow:false}};export default async function Page(){const s=await requireChannelUser();const{data}=await createAdminClient().from("usuario").select("nombre,email,ultimo_acceso_en,aseguradora(nombre_comercial),canal(nombre)").eq("id",s.actorId).single();return <AdminPage eyebrow="Cuenta interna" title="Mi perfil" description="Datos de tu acceso como usuario de canal."><section className="glass-panel mt-7 p-5 sm:p-6"><dl className="grid gap-5 sm:grid-cols-2"><Detail label="Nombre" value={data?.nombre}/><Detail label="Correo" value={data?.email}/><Detail label="Aseguradora" value={rel(data?.aseguradora,"nombre_comercial")}/><Detail label="Canal" value={rel(data?.canal,"nombre")}/></dl><p className="mt-6 border-t border-white/10 pt-5 text-xs leading-5 text-white/45">La asignación de aseguradora, canal y perfil solo puede modificarla el administrador de tu aseguradora.</p></section></AdminPage>}function Detail({label,value}:{label:string;value?:string|null}){return <div><dt className="text-[10px] font-bold uppercase tracking-wider text-white/40">{label}</dt><dd className="mt-1 font-semibold">{value||"—"}</dd></div>}function rel(v:unknown,k:string){const r=Array.isArray(v)?v[0]:v;return r&&typeof r==="object"&&k in r?String((r as Record<string,unknown>)[k]):"—"}
