import type{Metadata}from"next";import Link from"next/link";import{ArrowRight,FilePlus2}from"lucide-react";import{AdminPage}from"@/components/admin/admin-ui";import{requireChannelUser}from"@/lib/auth/session";import{createAdminClient}from"@/lib/supabase/admin";
export const metadata:Metadata={title:"Cotizaciones del canal | Arkad",robots:{index:false,follow:false}};export default async function Page({searchParams}:{searchParams:Promise<{estado?:string}>}){const s=await requireChannelUser();const{estado}=await searchParams;let query=createAdminClient().from("cotizacion").select("id,creado_en,estado,anios_vigencia,cuota_fija_mensual,cliente(nombre_razon_social,identificacion),vehiculo(marca,modelo),producto(nombre),usuario(nombre)").eq("canal_id",s.channelId!).order("creado_en",{ascending:false});if(["PENDIENTE","ACEPTADA","RECHAZADA","EXPIRADA"].includes(estado||""))query=query.eq("estado",estado!);const{data}=await query;return <AdminPage eyebrow="Producción del canal" title="Cotizaciones" description="Consulta las cotizaciones asistidas registradas desde este canal." action={<Link href="/canal/cotizaciones/nueva" className="inline-flex min-h-12 items-center justify-center gap-2 rounded-full bg-white px-5 text-sm font-bold text-[#071426]"><FilePlus2 className="size-4"/>Nueva cotización</Link>}><nav className="mt-7 flex flex-wrap gap-2">{[["","Todas"],["PENDIENTE","Pendientes"],["ACEPTADA","Aceptadas"],["RECHAZADA","Rechazadas"]].map(([value,label])=><Link key={value} href={value?`?estado=${value}`:"/canal/cotizaciones"} className={`min-h-10 rounded-full border px-4 py-2.5 text-xs font-bold ${estado===value||(!estado&&!value)?"border-cyan-100/40 bg-cyan-100/10":"border-white/12"}`}>{label}</Link>)}</nav><section className="mt-4 grid gap-3">{(data||[]).map(x=><Link key={x.id} href={`/canal/cotizaciones/${x.id}`} className="glass-panel grid gap-4 p-5 sm:grid-cols-[1fr_auto] sm:items-center"><div><p className="font-bold">{rel(x.cliente,"nombre_razon_social")}</p><p className="mt-1 text-sm text-white/55">{rel(x.vehiculo,"marca")} {rel(x.vehiculo,"modelo")} · {rel(x.producto,"nombre")}</p><p className="mt-2 text-xs text-white/38">{new Date(x.creado_en).toLocaleDateString("es-EC")} · Agente: {rel(x.usuario,"nombre")}</p></div><div className="flex items-center justify-between gap-4 sm:text-right"><div><p className="text-xs text-white/42">Cuota mensual</p><p className="mt-1 text-lg font-bold">{money(x.cuota_fija_mensual)}</p><p className="mt-1 text-[10px] font-bold text-cyan-100/65">{x.estado}</p></div><ArrowRight className="size-5 text-white/35"/></div></Link>)}{!data?.length&&<div className="glass-panel p-8 text-center text-sm text-white/50">No existen cotizaciones con este filtro.</div>}</section></AdminPage>}
function rel(v:unknown,k:string){const r=Array.isArray(v)?v[0]:v;return r&&typeof r==="object"&&k in r?String((r as Record<string,unknown>)[k]):"—"}function money(v:number|string){return new Intl.NumberFormat("es-EC",{style:"currency",currency:"USD"}).format(Number(v))}
