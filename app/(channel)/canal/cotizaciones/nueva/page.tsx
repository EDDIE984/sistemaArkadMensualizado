import type{Metadata}from"next";import Link from"next/link";import{redirect}from"next/navigation";import{ArrowRight,Users}from"lucide-react";import{QuoteWizard}from"@/components/quotes/quote-wizard";import{requireChannelUser}from"@/lib/auth/session";import{createAdminClient}from"@/lib/supabase/admin";import{getQuoteFormData}from"@/lib/quotes/data";
export const metadata:Metadata={title:"Nueva cotización asistida | Arkad",robots:{index:false,follow:false}};export default async function Page({searchParams}:{searchParams:Promise<{cliente?:string}>}){const s=await requireChannelUser();const{cliente}=await searchParams;const db=createAdminClient();if(!cliente){const{data}=await db.from("canal_cliente").select("cliente(id,nombre_razon_social,identificacion,email)").eq("canal_id",s.channelId!).order("creado_en",{ascending:false});return <main className="relative z-10 mx-auto w-full max-w-[980px] px-4 py-8 sm:px-7 lg:px-10"><p className="text-xs font-bold uppercase tracking-wider text-cyan-100/65">Cotización asistida</p><h1 className="mt-2 text-4xl font-bold tracking-[-0.035em]">Selecciona al cliente.</h1><p className="mt-2 text-sm text-white/58">La cotización quedará registrada a nombre del cliente y con tu usuario como agente.</p><div className="mt-7 grid gap-3">{(data||[]).map(x=>{const c=first(x.cliente);return c?<Link key={c.id} href={`/canal/cotizaciones/nueva?cliente=${c.id}`} className="glass-panel flex min-h-20 items-center justify-between gap-4 p-5"><div><p className="font-bold">{c.nombre_razon_social}</p><p className="mt-1 text-xs text-white/45">{c.identificacion} · {c.email}</p></div><ArrowRight className="size-5 text-cyan-100"/></Link>:null})}{!data?.length&&<div className="glass-panel p-8 text-center"><Users className="mx-auto size-7 text-cyan-100"/><p className="mt-4 font-bold">Primero vincula un cliente</p><Link href="/canal/clientes" className="mt-4 inline-flex min-h-11 items-center rounded-full bg-white px-5 text-sm font-bold text-[#071426]">Ir a clientes</Link></div>}</div></main>}const{data:link}=await db.from("canal_cliente").select("cliente(id,nombre_razon_social,identificacion,telefono,ciudad_id,fecha_nacimiento,genero,estado_civil)").eq("canal_id",s.channelId!).eq("cliente_id",cliente).maybeSingle();const client=first(link?.cliente||null);if(!client)redirect("/canal/clientes");const complete=Boolean(client.identificacion&&client.telefono&&client.ciudad_id&&client.fecha_nacimiento&&client.genero&&client.estado_civil);if(!complete)redirect("/canal/clientes?incompleto=1");const data=await getQuoteFormData(client.id,{insurerId:s.insurerId!,channelId:s.channelId!});return <main className="relative z-10 mx-auto w-full max-w-[1080px] px-4 py-8 sm:px-7 lg:px-10"><p className="text-xs font-bold uppercase tracking-wider text-cyan-100/65">Cotización asistida</p><h1 className="mt-2 text-4xl font-bold tracking-[-0.035em]">Cotizar para {client.nombre_razon_social}</h1><p className="mt-2 text-sm text-white/58">Productos disponibles exclusivamente para tu canal y aseguradora.</p><QuoteWizard data={data} mode="channel" clientId={client.id}/></main>}
function first<T>(v:T|T[]|null){return Array.isArray(v)?v[0]||null:v}
